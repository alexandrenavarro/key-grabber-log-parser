# key-grabber-log-parser

The key-grabber-log-parser project can parse the file LOG.TXT generated by a key grabber like the "KeyGrabber Forensic
Keylogger Ultra" and generates summary and statistics reports.
